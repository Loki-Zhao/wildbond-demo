# Wildbond Demo

2D pet collection and turn-based battle web game demo built with React, TypeScript, and Vite.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The GitHub Pages workflow builds with `GITHUB_PAGES=true` so the deployed asset paths use `/wildbond-demo/`.
